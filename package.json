{
  "name": "@phasereditor2d/scripts-simple-animations",
  "version": "1.0.0",
  "description": "Simple animation scripts for Phaser Editor 2D.",
  "main": "out/index.js",
  "types": "types/index.d.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "tsc --watch",
    "clean": "rm -Rf out/* && rm -Rf types/*",
    "build": "npm run clean && tsc",
    "publish": "npm run build && npm publish --access=public",
    "publish-local": "npm run build && npm publish --access=public --registry http://localhost:4873/",
    "patch-alpha": "npm version prerelease --preid=alpha --no-git-tag-version",
    "publish-alpha": "npm run build && npm publish --tag alpha --access=public",
    "publish-alpha-local": "npm run build && npm publish --tag alpha --access=public --registry http://localhost:4873/"
  },
  "keywords": [
    "animations",
    "scripts",
    "phasereditor2d",
    "phaser"
  ],
  "author": "Phaser Editor 2D",
  "license": "MIT",
  "devDependencies": {
    "@phasereditor2d/scripts-core": "^1.0.6",
    "phaser": "^3.70.0",
    "typescript": "^5.2.2"
  }
}
